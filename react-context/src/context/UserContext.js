import React from 'react';

// this userContext is nothing just a data provider which provides a data to diff. components like passing the props to nested components

const UserContext = React.createContext()

export default UserContext;

